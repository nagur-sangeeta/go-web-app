server {
    listen 80;
    server_name go-web-app.local;

    location / {
        proxy_pass http://go-web-app:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

